import React from 'react';
import { View, Text } from 'react-native';

export default function camera() {
    return (
        <View>
            <Text> camera </Text>
        </View>
    );
}